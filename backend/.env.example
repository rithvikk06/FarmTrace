# Solana RPC endpoint. Defaults to localhost if not provided.
SOLANA_RPC_URL="https://api.devnet.solana.com"

# The private key of the wallet that has the authority to validate farm plots.
# IMPORTANT: This should be in BYTE ARRAY format (e.g., [1,2,3,...,64])
VALIDATOR_PRIVATE_KEY="[...]"
GEE_SERVICE_ACCOUNT=""
GEE_PRIVATE_KEY_PATH=""
GEMINI_API_KEY=